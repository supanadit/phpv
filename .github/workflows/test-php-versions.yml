name: Test PHP Versions

on:
  push:
    branches:
      - development

jobs:
  test-php-versions:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php-version:
          - "4.4"
          - "5.0"
          - "5.1"
          - "5.2"
          - "5.3"
          - "5.4"
          - "5.5"
          - "5.6"
          - "7.1"
          - "7.2"
          - "7.3"
          - "7.4"
          - "8.0"
          - "8.1"
          - "8.2"
          - "8.3"
          - "8.4"
          - "8.5"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Cache phpv downloads
        uses: actions/cache@v4
        with:
          path: ~/.phpv/cache
          key: phpv-cache-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            phpv-cache-

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libssl-dev \
            libcurl4-openssl-dev \
            libxml2-dev \
            libonig-dev \
            libzip-dev \
            libsqlite3-dev \
            pkg-config \
            cmake \
            perl \
            m4 \
            autoconf \
            automake \
            libtool \
            re2c \
            bison \
            git \
            wget \
            curl \
            xz-utils \
            libgdbm-dev \
            libdb-dev \
            libreadline-dev \
            libtinfo5

      - name: Build phpv binary
        run: |
          go build -ldflags "-X github.com/supanadit/phpv/domain.AppVersion=ci-test" -o phpv ./app/phpv.go
          chmod +x phpv
          sudo mv phpv /usr/local/bin/

      - name: Initialize phpv
        run: |
          eval "$(phpv init bash)"
          echo "PHPV_ROOT=$PHPV_ROOT"
          echo "PATH=$PATH"
          # Persist for subsequent steps
          echo "PHPV_ROOT=$PHPV_ROOT" >> $GITHUB_ENV
          echo "$PHPV_ROOT/bin" >> $GITHUB_PATH

      - name: Download PHP ${{ matrix.php-version }} source
        timeout-minutes: 30
        run: |
          echo "Downloading PHP ${{ matrix.php-version }} source..."
          phpv download ${{ matrix.php-version }} -V
        env:
          PHPV_ROOT: ${{ env.PHPV_ROOT }}

      - name: Build PHP ${{ matrix.php-version }}
        timeout-minutes: 60
        run: |
          echo "Building PHP ${{ matrix.php-version }}..."
          phpv build ${{ matrix.php-version }} -V
        env:
          PHPV_ROOT: ${{ env.PHPV_ROOT }}

      - name: Test Shims
        run: |
          echo "Testing shims..."
          ls -la $PHPV_ROOT/bin/
          
          # Check if php shim exists
          if [ -f "$PHPV_ROOT/bin/php" ]; then
            echo "✓ php shim exists"
          else
            echo "✗ php shim not found"
            exit 1
          fi
          
          # Check if other shims exist
          for binary in php-cgi php-config phpize phpdbg; do
            if [ -f "$PHPV_ROOT/bin/$binary" ]; then
              echo "✓ $binary shim exists"
            else
              echo "✗ $binary shim not found"
              exit 1
            fi
          done
          
          echo "All shims verified successfully!"

      - name: Switch Version and Verify
        run: |
          echo "Switching to PHP ${{ matrix.php-version }}..."
          
          # Use phpv to switch version
          eval "$(phpv use ${{ matrix.php-version }})"
          echo "PHPV_VERSION=$PHPV_VERSION"
          
          # Verify version is set
          if [ -z "$PHPV_VERSION" ]; then
            echo "✗ PHPV_VERSION is not set"
            exit 1
          fi
          
          # Check that the version matches what we expect
          EXPECTED_VERSION="${{ matrix.php-version }}"
          if [[ "$PHPV_VERSION" == $EXPECTED_VERSION* ]]; then
            echo "✓ Version matches: $PHPV_VERSION"
          else
            echo "✗ Version mismatch. Expected: $EXPECTED_VERSION, Got: $PHPV_VERSION"
            exit 1
          fi

      - name: Run php -v
        run: |
          echo "Running php -v..."
          
          # Switch to the version first
          eval "$(phpv use ${{ matrix.php-version }})"
          
          # Run php -v
          PHP_OUTPUT=$(php -v 2>&1)
          echo "$PHP_OUTPUT"
          
          # Verify output contains PHP version
          EXPECTED_PATTERN="PHP ${{ matrix.php-version }}"
          if echo "$PHP_OUTPUT" | grep -q "$EXPECTED_PATTERN"; then
            echo "✓ php -v output contains expected version: $EXPECTED_PATTERN"
          else
            echo "✗ php -v output does not contain expected version: $EXPECTED_PATTERN"
            echo "Full output:"
            echo "$PHP_OUTPUT"
            exit 1
          fi
          
          echo "PHP ${{ matrix.php-version }} test completed successfully!"

      - name: Test additional PHP binaries
        run: |
          echo "Testing additional PHP binaries..."
          
          # Switch to the version
          eval "$(phpv use ${{ matrix.php-version }})"
          
          # Test php-config
          echo "Testing php-config..."
          php-config --version
          
          # Test phpize (check if it runs without errors)
          echo "Testing phpize..."
          phpize --version || true
          
          # Test phpdbg (if available for this version)
          echo "Testing phpdbg..."
          phpdbg --version 2>&1 || echo "phpdbg not available for this version"
          
          echo "Additional binaries test completed!"

      - name: Upload build artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: php-${{ matrix.php-version }}-build-logs
          path: |
            ~/.phpv/sources/
            ~/.phpv/versions/
          retention-days: 3

  test-summary:
    needs: test-php-versions
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Test Summary
        run: |
          echo "=========================================="
          echo "PHP Version Testing Summary"
          echo "=========================================="
          echo ""
          echo "Tested PHP versions:"
          echo "  - 4.4, 5.0, 5.1, 5.2, 5.3, 5.4, 5.5, 5.6"
          echo "  - 7.1, 7.2, 7.3, 7.4"
          echo "  - 8.0, 8.1, 8.2, 8.3, 8.4, 8.5"
          echo ""
          echo "Test scenario completed for each version:"
          echo "  1. Build - Compiled PHP from source"
          echo "  2. Test Shims - Verified shim binaries exist"
          echo "  3. Switch Version - Activated version with 'phpv use'"
          echo "  4. Run 'php -v' - Verified version output"
          echo ""
          echo "Overall status: ${{ needs.test-php-versions.result }}"
          echo "=========================================="
